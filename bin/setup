#! /usr/bin/env bash

set -e

# Load rvm
. ~/.rvm/scripts/rvm

# Use the right Ruby
rvm use ruby 1.9.3

bundle install --deployment

pushd theme/static/sass
  bundle exec bourbon install
  bundle exec neat install
popd
