image:
  file: .gitpod.Dockerfile

tasks:
  - name: Lume Server
    command: lume --serve

vscode:
  extensions:
    - denoland.vscode-deno

ports:
  - port: 3000
    onOpen: open-browser
    visibility: private